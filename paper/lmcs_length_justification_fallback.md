# LMCS Length Decision

The local LMCS build on 2026-05-16 is 47 pages.  The current version keeps the
theorem-to-artifact table and expanded proof contracts in the main text while
remaining below the earlier 50-page fallback target.

Editor-facing note if a length rationale is requested:

> The manuscript includes explicit definitions of the fixed sealed-extension calculus, the
> dependent objects `Real_k(X)`, the structural horn grammar, the horn-to-open-box adequacy
> theorem, the total-extension contractibility distinction, and the replacement theorem used
> to eliminate higher structural trace from minimal public signatures.  The expanded proof
> contracts remain in the main text so that the theorem stack and artifact status are
> auditable by referees.
